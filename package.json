{
  "name": "cicd-demo",
  "version": "1.0.0",
  "description": "Sample Node.js app for Jenkins CI/CD + Datadog demo",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "lint": "eslint src __tests__",
    "test": "jest --runInBand",
    "build": "node scripts/build.js",
    "package": "tar -czf build/artifact.tar.gz -C build .",
    "security": "npm audit --audit-level=high --production || true"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-standard": "^17.1.0",
    "eslint-plugin-import": "^2.29.0",
    "eslint-plugin-n": "^16.0.0",
    "eslint-plugin-promise": "^6.0.1",
    "jest": "^29.7.0"
  }
}

